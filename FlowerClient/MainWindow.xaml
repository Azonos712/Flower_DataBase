<Window x:Class="FlowerClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlowerClient"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"    
        Title="MainWindow" Height="720" Width="1300" TextElement.FontSize="14" Loaded="Window_Loaded" WindowStartupLocation="CenterScreen" Closed="Window_Closed">
    <Grid RenderTransformOrigin="0.561,0.367">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <DockPanel Grid.Column="0" Grid.Row="0">
            <Menu x:Name="main_menu" VerticalAlignment="Center">
                <MenuItem x:Name="admin_menuitem" Header="Администрирование" Height="20">
                    <MenuItem Header="Таблица пользователей" Click="MenuItem_Click"></MenuItem>
                </MenuItem>
                <MenuItem x:Name="system_menuitem" Header="Система" Height="20">
                    <MenuItem Header="Выход из учетной записи" Click="MenuItem_Click_1"></MenuItem>
                </MenuItem>
            </Menu>
        </DockPanel>

        <WrapPanel Margin="15,68.2,399.6,56.4" Grid.Row="1" RenderTransformOrigin="0.492,0.487" Grid.Column="0">
            <Canvas x:Name="panel1" Width="250" Height="227" Margin="25,15,0,0" Background="White">
                <materialDesign:Card RenderTransformOrigin="0.5 0.5" Visibility="Hidden" Width="250" Grid.Row="1" Height="227" HorizontalAlignment="Left" VerticalAlignment="Top" x:Name="card1" MouseLeftButtonDown="card1_MouseLeftButtonDown">
                    <materialDesign:Card.RenderTransform>
                        <ScaleTransform />
                    </materialDesign:Card.RenderTransform>
                    <materialDesign:Card.Style>
                        <Style TargetType="{x:Type materialDesign:Card}">
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="Mouse.MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.2" Duration="00:00:00.3" />
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.2" Duration="00:00:00.3" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="Mouse.MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="00:00:00.3" />
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="00:00:00.3" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </materialDesign:Card.Style>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="0" />
                            <RowDefinition Height="227.2" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Orientation="Horizontal" Margin="2,13,2.6,0" Height="153" VerticalAlignment="Top" Width="245"/>

                        <StackPanel Margin="8,2,7.6,66.2" Grid.Row="1">
                            <Image Source="{Binding imageP}" Margin="0,0,0,0" Height="153"/>
                        </StackPanel>
                        <TextBlock FontWeight="Bold" Margin="6,166,9.6,41.2" Grid.RowSpan="2" Text="{Binding nameP}"/>
                        <TextBlock TextWrapping="Wrap" VerticalAlignment="Center" Margin="6,185,9.6,4.2" Grid.RowSpan="2" Text="{Binding captionP}"/>

                    </Grid>
                </materialDesign:Card>
            </Canvas>

            <Canvas x:Name="panel2" Width="250" Height="227" Margin="45,15,0,0" Background="White">
                <materialDesign:Card RenderTransformOrigin="0.5 0.5" Visibility="Hidden" Width="250" Grid.Row="1" Height="227" HorizontalAlignment="Left" VerticalAlignment="Top" x:Name="card2" MouseLeftButtonDown="card2_MouseLeftButtonDown">
                    <materialDesign:Card.RenderTransform>
                        <ScaleTransform />
                    </materialDesign:Card.RenderTransform>
                    <materialDesign:Card.Style>
                        <Style TargetType="{x:Type materialDesign:Card}">
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="Mouse.MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.2" Duration="00:00:00.3" />
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.2" Duration="00:00:00.3" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="Mouse.MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="00:00:00.3" />
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="00:00:00.3" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </materialDesign:Card.Style>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="0" />
                            <RowDefinition Height="227.2" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Orientation="Horizontal" Margin="2,13,2.6,0" Height="153" VerticalAlignment="Top" Width="245"/>

                        <StackPanel Margin="8,2,7.6,66.2" Grid.Row="1">
                            <Image Source="{Binding imageP}" Margin="0,0,0,0" Height="153"/>
                        </StackPanel>
                        <TextBlock FontWeight="Bold" Margin="6,166,9.6,41.2" Grid.RowSpan="2" Text="{Binding nameP}" />
                        <TextBlock TextWrapping="Wrap" VerticalAlignment="Center" Margin="6,185,9.6,4.2" Grid.RowSpan="2" Text="{Binding captionP}"/>

                    </Grid>
                </materialDesign:Card>
            </Canvas>

            <Canvas x:Name="panel3" Width="250" Height="227" Margin="45,15,0,0" Background="White" Panel.ZIndex="100">
                <materialDesign:Card RenderTransformOrigin="0.5 0.5" Visibility="Hidden" Width="250" Grid.Row="1" Height="227" HorizontalAlignment="Left" VerticalAlignment="Top" x:Name="card3" MouseLeftButtonDown="card3_MouseLeftButtonDown">
                    <materialDesign:Card.RenderTransform>
                        <ScaleTransform />
                    </materialDesign:Card.RenderTransform>
                    <materialDesign:Card.Style>
                        <Style TargetType="{x:Type materialDesign:Card}">
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="Mouse.MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.2" Duration="00:00:00.3" />
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.2" Duration="00:00:00.3" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="Mouse.MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="00:00:00.3" />
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="00:00:00.3" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </materialDesign:Card.Style>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="0" />
                            <RowDefinition Height="227.2" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Orientation="Horizontal" Margin="2,13,2.6,0" Height="153" VerticalAlignment="Top" Width="245"/>

                        <StackPanel Margin="8,2,7.6,66.2" Grid.Row="1">
                            <Image Source="{Binding imageP}" Margin="0,0,0,0" Height="153"/>
                        </StackPanel>
                        <TextBlock FontWeight="Bold" Margin="6,166,9.6,41.2" Grid.RowSpan="2" Text="{Binding nameP}"/>
                        <TextBlock TextWrapping="Wrap" VerticalAlignment="Center" Margin="6,185,9.6,4.2" Grid.RowSpan="2" Text="{Binding captionP}"/>

                    </Grid>
                </materialDesign:Card>
            </Canvas>

            <Canvas x:Name="panel4" Width="250" Height="227" Margin="25,30,0,0" Background="White">
                <materialDesign:Card RenderTransformOrigin="0.5 0.5" Visibility="Hidden" Width="250" Height="227" HorizontalAlignment="Left" VerticalAlignment="Top" x:Name="card4" MouseLeftButtonDown="card4_MouseLeftButtonDown">
                    <materialDesign:Card.RenderTransform>
                        <ScaleTransform />
                    </materialDesign:Card.RenderTransform>
                    <materialDesign:Card.Style>
                        <Style TargetType="{x:Type materialDesign:Card}">
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="Mouse.MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.2" Duration="00:00:00.3" />
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.2" Duration="00:00:00.3" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="Mouse.MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="00:00:00.3" />
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="00:00:00.3" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </materialDesign:Card.Style>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="0" />
                            <RowDefinition Height="227.2" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Orientation="Horizontal" Margin="2,13,2.6,0" Height="153" VerticalAlignment="Top" Width="245"/>

                        <StackPanel Margin="8,2,7.6,66.2" Grid.Row="1">
                            <Image Source="{Binding imageP}" Margin="0,0,0,0" Height="153"/>
                        </StackPanel>
                        <TextBlock FontWeight="Bold" Margin="6,166,9.6,41.2" Grid.RowSpan="2" Text="{Binding nameP}"/>
                        <TextBlock TextWrapping="Wrap" VerticalAlignment="Center" Margin="6,185,9.6,4.2" Grid.RowSpan="2" Text="{Binding captionP}"/>
                        <!--<Button Width="100" Height="20" Content="Подробнее" Margin="10,197,139.6,10.2" Grid.RowSpan="2" FontSize="10"></Button>
                        <Button Width="100" Height="20" Content="Изменить" Margin="140,197,9.6,10.2" Grid.RowSpan="2" FontSize="10"></Button>-->

                    </Grid>
                </materialDesign:Card>
            </Canvas>

            <Canvas x:Name="panel5" Width="250" Height="227" Margin="45,30,0,0" Background="White">
                <materialDesign:Card RenderTransformOrigin="0.5 0.5" Visibility="Hidden" Width="250" Height="227" HorizontalAlignment="Left" VerticalAlignment="Top" x:Name="card5" MouseLeftButtonDown="card5_MouseLeftButtonDown">
                    <materialDesign:Card.RenderTransform>
                        <ScaleTransform />
                    </materialDesign:Card.RenderTransform>
                    <materialDesign:Card.Style>
                        <Style TargetType="{x:Type materialDesign:Card}">
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="Mouse.MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.2" Duration="00:00:00.3" />
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.2" Duration="00:00:00.3" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="Mouse.MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="00:00:00.3" />
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="00:00:00.3" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </materialDesign:Card.Style>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="0" />
                            <RowDefinition Height="227.2" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Orientation="Horizontal" Margin="2,13,2.6,0" Height="153" VerticalAlignment="Top" Width="245"/>

                        <StackPanel Margin="8,2,7.6,66.2" Grid.Row="1">
                            <Image Source="{Binding imageP}" Margin="0,0,0,0" Height="153"/>
                        </StackPanel>
                        <TextBlock FontWeight="Bold" Margin="6,166,9.6,41.2" Grid.RowSpan="2" Text="{Binding nameP}"/>
                        <TextBlock TextWrapping="Wrap" VerticalAlignment="Center" Margin="6,185,9.6,4.2" Grid.RowSpan="2" Text="{Binding captionP}"/>

                    </Grid>
                </materialDesign:Card>
            </Canvas>

            <Canvas x:Name="panel6" Visibility="Hidden" Width="250" Height="227" Margin="45,30,0,0" Background="White">
                <materialDesign:Card RenderTransformOrigin="0.5 0.5" Visibility="Visible" Width="250" Height="227" HorizontalAlignment="Left" VerticalAlignment="Top" x:Name="card6" MouseLeftButtonDown="card6_MouseLeftButtonDown">
                    <materialDesign:Card.RenderTransform>
                        <ScaleTransform x:Name="trans6"/>
                    </materialDesign:Card.RenderTransform>
                    <materialDesign:Card.Style>
                        <Style TargetType="{x:Type materialDesign:Card}">
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="Mouse.MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.2" Duration="00:00:00.3" />
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.2" Duration="00:00:00.3" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="Mouse.MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="00:00:00.3" />
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="00:00:00.3" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </materialDesign:Card.Style>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="0" />
                            <RowDefinition Height="227.2" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Orientation="Horizontal" Margin="2,13,2.6,0" Height="153" VerticalAlignment="Top" Width="245"/>

                        <StackPanel Margin="8,2,7.6,66.2" Grid.Row="1">
                            <Image Source="{Binding imageP}" Margin="0,0,0,0" Height="153"/>
                        </StackPanel>
                        <TextBlock FontWeight="Bold" Margin="6,166,9.6,41.2" Grid.RowSpan="2" Text="{Binding nameP}"/>
                        <TextBlock TextWrapping="Wrap" VerticalAlignment="Center" Margin="6,185,9.6,4.2" Grid.RowSpan="2" Text="{Binding captionP}"/>
                    </Grid>
                </materialDesign:Card>
            </Canvas>
        </WrapPanel>

        <StackPanel x:Name="metadata" Visibility="Visible" Margin="925,71.2,8.6,12.4" Grid.Row="1" Width="360">
            <Grid Height="580">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"></ColumnDefinition>
                    <ColumnDefinition Width="180"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="author" Width="150" Margin="20,30,10,0" Grid.Column="0" Height="30" VerticalAlignment="Top"/>
                <ComboBox x:Name="exposition" Width="150" Margin="15,30,15,0" Grid.Column="1" Height="30" VerticalAlignment="Top"/>

                <ComboBox x:Name="name" Width="150" Margin="20,90,10,0" Grid.Column="0" Height="30" VerticalAlignment="Top"/>
                <ComboBox x:Name="life_form" Width="150" Margin="15,90,15,0" Grid.Column="1" Height="30" VerticalAlignment="Top"/>

                <ComboBox x:Name="people" Width="150" Margin="20,150,10,0" Grid.Column="0" Height="30" VerticalAlignment="Top"/>
                <ComboBox x:Name="history" Width="150" Margin="15,150,15,0" Grid.Column="1" Height="30" VerticalAlignment="Top"/>

                <ComboBox x:Name="buildings" Width="150" Margin="20,210,10,0" Grid.Column="0" Height="30" VerticalAlignment="Top"/>
                <ComboBox x:Name="category" Width="150" Margin="15,210,15,0" Grid.Column="1" Height="30" VerticalAlignment="Top"/>

                <ComboBox x:Name="group" Width="150" Margin="20,270,10,0" Grid.Column="0" Height="30" VerticalAlignment="Top"/>
                <ComboBox x:Name="eco_group" Width="150" Margin="15,270,15,0" Grid.Column="1" Height="30" VerticalAlignment="Top"/>

                <ComboBox x:Name="season" Width="150" Margin="20,330,10,135" Grid.Column="0" VerticalAlignment="Top"/>
                <ComboBox x:Name="year" Width="150" Margin="15,330,15,0" Grid.Column="1" Height="30" VerticalAlignment="Top"/>
                
                <Button Width="150" Margin="107,538,103,0" Content="Поиск" Click="Button_Click" VerticalAlignment="Top" Grid.ColumnSpan="2"/>

            </Grid>
            <!--<Label Content="Данные об изображении:" FontSize="22"></Label>
            <Label Content="Название вида:" FontSize="22" Margin="0, 10, 0, 0"></Label>
            <TextBlock Text="{Binding nameP}" Margin="5,5,5,0" FontSize="24" TextWrapping="Wrap" Height="66"></TextBlock>
            <Label Content="Автор:" FontSize="22" Margin="0, 10, 0, 0"></Label>
            <TextBlock Text="{Binding authorP}" Margin="5,5,5,0" FontSize="24" TextWrapping="Wrap" Height="66"></TextBlock>
            <Label Content="Экспозиция:" FontSize="22" Margin="0, 10, 0, 0"></Label>
            <TextBlock Text="{Binding expositionP}" Margin="5,5,5,0" FontSize="24" TextWrapping="Wrap" Height="66"></TextBlock>
            <Label Content="Категория:" FontSize="22" Margin="0, 10, 0, 0"></Label>
            <TextBlock Text="{Binding categoryP}" Margin="5,5,5.2,0" FontSize="24" TextWrapping="Wrap" Height="66"></TextBlock>-->

        </StackPanel>
        <Button x:Name="nextPage" Content="&gt;" HorizontalAlignment="Left" Margin="811,614.2,0,0" Grid.Row="1" VerticalAlignment="Top" Width="40" Height="30" Click="nextPage_Click"/>
        <Button x:Name="prevPage" Content="&lt;" HorizontalAlignment="Left" Margin="725,614.2,0,0" Grid.Row="1" VerticalAlignment="Top" Width="40" Click="prevPage_Click" Height="30"/>

    </Grid>
</Window>
